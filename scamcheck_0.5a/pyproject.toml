[project]
name = "scamcheck"
version = "0.1.0"
description = "Rule-based phishing email checker and domain email-config checker (EMCC), built for the OUPI Cyber Clinic Contest 2026."
requires-python = ">=3.9"
dependencies = [
    "dnspython>=2.6.0",
    "python-whois>=0.9.4",
    "flask>=3.0.0",
    "gunicorn>=22.0.0",
]

[tool.uv]
package = false
